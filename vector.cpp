#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<int > arr;
    arr.push_back(5);
    arr.push_back(5);
    arr.push_back(4);
    arr.push_back(3);
    // arr.push_back(2);
    //  sort(arr.begin(), arr.end());
    // auto it=lower_bound(arr.begin(), arr.end(), 2);
    // cout<<*it<<endl;
    // auto itt=upper_bound(arr.begin(), arr.end(), 6);
    // if(itt==arr.end())
    // cout<<"end"<<endl;
    // cout<<*itt<<endl;
    // sort(arr.begin(), arr.end(), greater<int>());
    // for(int i=0;i<arr.size();i++)
    // cout<<arr[i]<<" ";
    // reverse(arr.begin(), arr.end()); // to reverse the array 
   
// for static matrix

//    int row=5, col=5;
//     vector<vector<int>>matrix(row, vector<int>(col, -1));
//     for(auto val:matrix){
//         for(auto value:val)cout<<value<<" ";
//         cout<<endl;
//     }


// dynamic matrix creation 
// vector<vector<int>>matrix;
// for(int i=0;i<5;i++){
//     vector<int>temp;
//     for(int j=0;j<7;j++){
//         temp.push_back(i*j);
//     }
//     matrix.push_back(temp);
// }

// for(auto val:matrix){
//     for(auto value:val)
//     cout<<value<<" ";
//     cout<<endl;
// }



    
}